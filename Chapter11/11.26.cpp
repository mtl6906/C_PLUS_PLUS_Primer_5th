#include <map>
#include <iostream>
#include <string>

using std::string;
using std::map;
using std::map;
using std::pair;

int main()
{
        map<string,int> mp;

        int t = (mp["154"] = 5);        //      返回值int

        return 0;
}
