#include <iostream>
#include <map>
#include <string>
#include <vector>

using std::pair;
using std::map;
using std::cin;
using std::cout;
using std::string;
using std::endl;
using std::vector;

int main()
{
        vector<int> iv;
        string key;

        map<string,vector<int>> mp;

        pair<map<string,vector<int>>::iterator,bool> t = mp.insert({key,iv});

        return 0;
}
