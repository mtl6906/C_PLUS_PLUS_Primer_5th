#include <string>

using std::string;

int main()
{
        while(string::iterator iter != e.end()){}       //      当iter不等于e的尾后指针时，循环继续

//      while(bool status = find(word)){}               //      当find(word)返回true时，循环继续

//              if(!status){}   error，外部不可见

        bool status;

        while(status = find(word)){}

        if(!status){}   //


        return 0;
}
